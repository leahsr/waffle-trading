package de.thws.repository.constants

object Properties {
  object WaffleTransaction {
    val id = "transaction_id"
    val timestamp = "timestamp"
    val transactionType = "transaction_type"
    val quantity = "quantity"
    val userName = "user_name"
  }

  object Price {
    val id = "price_timestamp_id"
    val price = "price"
  }
}
